<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
  xmlns:dfdl="http://www.ogf.org/dfdl/dfdl-1.0/" targetNamespace="http://example.com">

  <xs:annotation>
    <xs:appinfo source="http://www.ogf.org/dfdl/dfdl-1.0/">
      <!-- NOTE: xs:format, i.e., wrong prefix
        We want file and line number to be of the annotation below, not the 
        xs:schema element above.
        Note: don't change the line number here. The test checks it. -->
      <xs:format separator="" initiator="" terminator=""
        leadingSkip='0' textTrimKind="none" initiatedContent="no"
        ignoreCase="no" representation="text" textNumberRep="standard"
        lengthKind="delimited" encoding="ASCII" alignment="implicit"
        alignmentUnits="bytes" trailingSkip="0" />
    </xs:appinfo>
  </xs:annotation>

  <xs:element name="x" type="xs:int" />


</xs:schema>
